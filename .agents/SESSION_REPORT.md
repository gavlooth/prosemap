# Session report

## 2026-09-15T12:01:10+03:00 — Prosemap repository prepared

- Objective: user requested a named sibling repository with basic boilerplate and the necessary plan/architecture files, following the decision to reject Python and use Bun/TypeScript.
- Workspace: `/home/heefoo/Documents/code/prosemap`; independent Git-backed jj repository initialized with `jj git init`. No remote, bookmark publication or license selected.
- Changes: added private package manifest, strict TypeScript configuration, Bun lockfile, scoped generated/secret ignores, runnable help/version CLI, README, standalone adapted design plan, architecture document and artifact-contract specification. Existing source repository and manuscript were not modified by this preparation.
- Coverage: 100% of requested repository preparation; implemented CLI supports help/version and invalid-argument rejection. Readability analyzers, LLM judgment, rendering runners and analysis artifacts are explicitly planned rather than implemented. This is not a claim of implementation coverage for the proposed full tool.
- Decision: Bun/TypeScript core; parse5, Zod and Puppeteer selected/proposed for their future first consumers, not installed as unused runtime dependencies. Python exporter in the sibling project is research reference only, not a Prosemap runtime dependency. No provider SDK or credentials configured.
- Verification: Bun 1.4.0 available; `bun install` installed five packages including TypeScript 5.9.3 and Bun types 1.4.2. `bun run check` passed; actual `bun run start --help` and `--version` succeeded (version 0.1.0); `bun install --frozen-lockfile` passed unchanged. Throwaway Eval process invocation `bun src/cli.ts analyze` returned exit 2, empty stdout and the unsupported-arguments error. Plan-transfer check confirmed final summary, roadmap and held-out evaluation present.
- Cleanup: no temporary scripts, generated builds or dummy analysis modules retained. Dependencies are ignored; lockfile, source and handoff files remain versioned. No permanent tests added for help text or incidental wiring.
- Negative memory/limits: standalone pseudo-table, ASCII and Delta detectors have not been located; historical audit source revision and literature references remain unverified. Browser compatibility and LLM behavior have not been exercised because those integrations are not part of this preparation.
- Next action: execute PLAN.md Phase 0, freeze corpus provenance/calibration/held-out splits and inventory reusable checks; then implement runtime contracts and source-preserving HTML extraction. Keep contextual judgments evidence-backed and advisory.
- Blockers/restart requirements: none for running the prepared CLI. Future browser/model integrations require their own dependencies and focused verification. Required `jj describe` follows immediately; no push requested.

Signature: openai-codex/gpt-6-astra

## 2026-09-16T12:41:59+03:00 — Full Prosemap roadmap implemented

- Objective: implement the complete roadmap as an executable Bun/TypeScript CLI while preserving exact evidence, advisory boundaries and narrow CI semantics.
- Workspace: `/home/heefoo/Documents/code/prosemap`; sibling `../christos-cloudflare-page` remained research-only and was not modified.
- Updated: 2026-09-16T13:59:29+03:00 after a correctness and coverage review.
- Changes: implemented versioned Zod contracts; parse5 source-preserving extraction; structure/reference/definition/terminology/notation/formula/source-pattern analyzers; documented lexical cohesion observations; route-aware evidence retrieval; opt-in provider-neutral contextual HTTP judgments; isolated Puppeteer surface checks; immutable JSONL/Markdown run artifacts; deterministic reporting; compatible-run comparison; replay-verified allowlisted CI gating; CLI commands `analyze`, `report`, `compare`, `gate` and `evaluate`; calibrated/regression/held-out fixtures; asset, metric and evaluation documentation. Follow-up corrections fixed real DOM math delimiter detection, gave missing rendered math its own rule, restored strong/b term candidates, emitted explicit-definition and eligible-ARI observations, replaced parse5 node mutation with a WeakMap, cached validated configs once, normalized imports, and consolidated stable JSON/SHA utilities.
- Provenance result: export snapshot `f18a599279e8e74e6acba32701e5ec50b7377b55` has source SHA-256 `74f42364f5c57e3e58bdf49cd4f8e15b9035901a13223f498e23300da3f99f0d`. The manual readability report has no source hash/revision and is not ground truth. The separate immutable sentence-audit source hash is `704021172f8c15bd25222428bd0ef59bb0b3bf47130e224854cc0aac3f61d203`; that audit is paused at 11,407/14,187 eligible records (80.40%).
- Commands run: dependency installation/removal; repeated `bun run check`; `bun test`; source-only, contextual-loopback and Chromium `analyze` runs; `evaluate fixtures/manifest.json`; comparable `compare` and allowlisted `gate` runs; forged/omitted artifact probes; popup-network probe; corrected definition/ARI report smoke; `bun audit`; `jj status`; `jj diff --stat`.
- Verification: release `tsc --noEmit` passed; `bun test` passed 26 tests/98 expectations across 6 files, including real Chromium surface behavior; fixture evaluation produced 8 true-positive rule/document labels, 7 advisory/neutral false-positive labels, 0 misses, precision 0.5333 and recall 1.0 across 3 synthetic documents; held-out tuning use was rejected; missing inline/display/dollar math, MathJax error labels, rendered controls, popup/network isolation and mapped/unmapped surface evidence were exercised; contextual bad citations, document-hash mismatch, redirect, oversize and abstention paths were exercised; artifacts/report/evaluation boundaries and comparison moves/splits/duplicates/incompatibilities were exercised; eligible `terminology.definition` and `formula.ari` records appeared in an actual CLI report; `bun audit` found no vulnerabilities across 30 packages.
- Security: focused review found popup-target network bypass and unauthenticated artifact completeness risks. Fixes added a browser-wide dead proxy, no loopback bypass, popup closure, disabled non-proxied WebRTC UDP, independently selected source validation, deterministic analyzer replay for both gate inputs and comparison recomputation. Re-review found no remaining concrete bypass in those paths. Surface/model findings remain advisory because the gate cannot independently replay them.
- Negative memory/limits: the first implementation had an inert double-backslash surface delimiter regex, mislabeled missing-rendered-math findings, dead strong/b term detection, dead definition/ARI finding paths, parse5 node mutation, repeated config reads, duplicate canonical/hash helpers, stale status prose and insufficient boundary tests. These were corrected and captured by behavior tests. Lexical cohesion emitted two neutral observations with no defect labels and therefore precision 0 under defect-label evaluation; it is descriptive only and excluded from CI. Intentional notation and route controls produced advisory observations, confirming that heuristics must not gate. A source span proves a cited excerpt, not completeness; completeness for CI is established by deterministic replay. Live model quality, repeatability, usage/cost and human ordinal agreement remain unmeasured.
- Current recommendation: use source-only mechanical runs and the two-rule replayable CI allowlist (`reference.unresolved`, `structure.duplicate-id`) now. Before promoting contextual or cohesion findings, obtain independently annotated documents and run a fixed live-provider evaluation. Add a separately rerunnable surface/PDF gate contract before allowing rendering rules in CI.
- Unresolved issues: independent annotator data, live-provider/model selection, whole-book contextual cost, comprehension study and a PDF/Pandoc adapter are not supplied. These block efficacy claims, not the implemented CLI.
- Dependencies/restart requirements: Bun >=1.4.0; installed runtime dependencies are Zod, parse5 and puppeteer-core. Surface runs require an installed Chromium path (verified with `/usr/bin/chromium`). Contextual runs require an explicit HTTPS or literal-loopback endpoint and optional environment token. No restart required.
- Next actions: collect adjudicated held-out labels; evaluate contextual precision/ordinal agreement/repeatability/cost; retain or remove heuristic indices based on that evidence; implement an independently replayable generated-edition/PDF gate only if CI needs rendering enforcement.

Signature: openai-codex/gpt-5.6-sol

## 2026-09-18T00:00:00+03:00 — Bend v2 implementation completion

- Objective: finish every remaining implementation item in `~/.claude/plans/mutable-brewing-whisper.md` for the deterministic Bend port.
- Workspace: `/home/heefoo/Documents/code/prosemap`; changed the Bend core, parser, metrics, gate/evaluation paths, artifact persistence, tests, plan, and handoff report.
- Code changes: added true UTF-8 byte encoding/slicing and UTF-8 SHA hashing; unified exact `T.Metric` output and terminology density; added the rule-ID prefix law and proof; added inline/display/fenced Markdown math extraction and command-aware TeX tokenization; added replay-verified gate/evaluate flows; added a completion-marker artifact commit protocol because Base has no atomic rename primitive.
- Coverage: 100% of the requested remaining implementation items; the optional TypeScript parity cross-check was also executed and documented as non-comparable across source surfaces.
- Commands run: all core Bend module checks; `bend PROOF.bend`; Bend smoke suite including `utf8_test` and `math_test`; end-to-end analyze with committed marker; replay `gate`; `evaluate`; `bun run check`; `bun test`; `pandoc README.md`; and the TypeScript CLI analyzer.
- Key results: all checked Bend modules and laws printed `All terms check.`; UTF-8 fixture passed multibyte lengths/slices/hashes; math fixture exposed inline/display/fenced TeX and emitted `Case-similar TeX symbols`; analyze produced report/findings/manifest plus completion marker; replay gate returned `GATE PASS (0 violation(s))`; evaluate returned `EVALUATE PASS (1 comparison record(s))`; Bun passed 26 tests / 98 expectations.
- Cross-check result: the pandoc-converted README ran through TypeScript successfully, but its HTML surface produced route/cohesion findings not directly comparable with the Bend Markdown-source run; no false parity claim is made.
- Negative memory/limits: direct recursive TeX scanning over computed remainders violated Bend termination, so the parser now uses a fuel-bounded state machine; mutual-recursive tokenizer helpers were inlined to satisfy Bend's no-mutual-recursion rule. Base lacks atomic rename, so commit visibility is marker-gated rather than filesystem-rename atomic.
- Current recommendation: retain the completion marker as the documented artifact commit contract; treat TypeScript/Bend parity as surface-dependent until a normalized shared fixture is defined.
- Unresolved issues: HTTPS contextual transport remains intentionally deferred; no requested implementation item remains.
- Dependencies/restart requirements: `/home/heefoo/.bend/bin/bend` and Bun are installed; no external service is required for the verified paths.

Signature: openai-router/deepseek/deepseek-v4.1-flash

## 2026-09-18T15:54:23+03:00 — TypeScript replaced by Bend; repository published

- Objective: replace the TypeScript implementation completely with the Bend implementation, move Bend to the repository root, create `gavlooth/prosemap` on GitHub if absent (public), and push.
- Workspace: `/home/heefoo/Documents/code/prosemap`. New layout: `prosemap/*.bend`, `LAWS.bend`, `PROOF.bend`, `README.md`, `docs/`, `fixtures/`.
- Code changes: moved `bend/prosemap` to `prosemap/` and `bend/{LAWS,PROOF}.bend` to the root (relative imports were already root-compatible, so no source edit was needed); deleted `src/`, `test/`, `ci/`, `package.json`, `tsconfig.json`, `bun.lock`, the `.gitignore`-tracked `node_modules/`, the dead `prosemap/proof_scratch.bend` (its lemmas are superseded by `prosemap/proof_lemmas.bend`), and an empty `prosemap/effs/`; rewrote `README.md` for the Bend pipeline; rewrote `docs/{ARCHITECTURE,CONTRACTS,METRICS,EVALUATION,ASSET_INVENTORY,PLAN}.md` to the Bend reality; added the `main` bookmark and the `origin` remote.
- Coverage: 100% of the requested cutover. Replaced: implementation, tests, CLI contract, docs. Deliberately retained: `fixtures/` (frozen research assets documented as unconsumed by the Bend pipeline).
- Commands run: `bend PROOF.bend`; per-module `bend prosemap/<mod>.bend` for 15 modules; the full smoke suite (`test`, `sha_test`, `utf8_test`, `mech_test{,2,3}`, `md_test`, `math_test`, `gate_test`, `cmp2_test`, `ctx_test`, `i3_test`, `jr_test`); end-to-end `analyze` into two run directories; legacy gate; replay gate with matching and forged candidate findings; `evaluate` with explicit paths and with `PROSEMAP_BASE_RUN`/`PROSEMAP_CANDIDATE_RUN` directories; the `grep -q '^GATE PASS'` CI idiom; `gh repo create gavlooth/prosemap --public`.
- Key results: all 15 modules and `bend PROOF.bend` print `All terms check.`; all 13 smoke programs pass; analyze writes `report.md`, `findings.jsonl`, `manifest.json` plus the completion marker; legacy gate `GATE PASS (0 violation(s))`; replay gate `GATE PASS (0 violation(s))` on matching artifacts and `GATE FAIL (replay verification failed)` on a forged candidate; `EVALUATE PASS (3 comparison record(s))` for both input styles; CI grep idiom observed; `gavlooth/prosemap` created public (empty, default branch unset until the first push).
- Negative memory/limits: the cutover loses three TypeScript capabilities that Bend does not implement — HTML ingestion (parse5), the Chromium surface stage (puppeteer-core), and HTTPS/remote contextual transport. Input is Markdown only (pandoc bridges formats), contextual transport is loopback plaintext TCP, and there is no exit-code policy: PASS/FAIL is stdout text, so CI must wrap the command (`| grep -q '^GATE PASS'`), unlike the previous TS gate that exited 1. The retained HTML fixtures and their configs are no longer consumed by any pipeline, so their labels cannot be reported as Bend evaluation results.
- Current recommendation: treat `prosemap/main.bend` plus `LAWS.bend`/`PROOF.bend` as the implementation of record; before reusing the retained HTML assets as evaluation data, specify an HTML-to-Markdown bridge and source-span equivalence; if CI needs a real failure signal, add an FFI exit path or keep the grep wrapper.
- Unresolved issues: no exit-code channel for the gate; HTML ingestion, rendered-surface checks and remote TLS transport remain deferred; corpus-level rule evaluation on Markdown labels is still open.
- Next actions: verify the pushed bookmark on GitHub; build a frozen Markdown corpus with reviewed labels; decide whether the retained HTML fixtures stay as provenance or get retired.
- Dependencies/restart requirements: `/home/heefoo/.bend/bin/bend` (Bend 2.0.5). Bun, Node, Bun dependencies and `node_modules/` are no longer required by the implementation. No external service required.

Signature: openai-router/deepseek/deepseek-v4.1-flash
